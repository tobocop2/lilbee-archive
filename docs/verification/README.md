# Hardware verification captures

Run output from real hardware, kept verbatim so a claim in a pull request or an
issue can be checked against what the machine actually printed rather than
against a summary of it.

Committed as Markdown on purpose: the repository ignores `*.txt`, which silently
swallowed an earlier attempt to commit these and left several "verified on
hardware" claims resting on files that existed only on one laptop.
