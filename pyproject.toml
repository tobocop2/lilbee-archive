[project]
name = "lilbee"
version = "0.2.0"
description = "Local RAG for the terminal. Drop files in a folder, ask questions, get answers. JSON API for coding agents."
requires-python = ">=3.11"
dependencies = [
    "ollama",
    "fastembed>=0.6",
    "lancedb",
    "pymupdf4llm",
    "tree-sitter>=0.25",
    "tree-sitter-language-pack>=0.7",
    "typer",
    "tiktoken",
    "pymupdf-layout>=1.27.1",
    "prompt-toolkit>=3.0",
    "python-docx>=1.1",
    "openpyxl>=3.1",
    "python-pptx>=1.0",
    "ebooklib>=0.18",
    "beautifulsoup4>=4.12",
    "pytesseract>=0.3",
    "Pillow>=10.0",
    "mcp>=1.26.0",
]

[project.scripts]
lilbee = "lilbee.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lilbee"]

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pexpect>=4.9.0",
    "pytest",
    "pytest-cov",
    "reportlab",
    "ruff>=0.15.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["lilbee"]
omit = ["src/lilbee/__main__.py"]

[tool.coverage.report]
show_missing = true
fail_under = 100

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes
    "I",     # isort
    "UP",    # pyupgrade
    "B",     # flake8-bugbear
    "SIM",   # flake8-simplify
    "RUF",   # ruff-specific
]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
ignore_missing_imports = true
